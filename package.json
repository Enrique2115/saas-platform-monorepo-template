{
  "name": "with-tailwind",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "pnpm exec ultracite lint",
    "check-types": "turbo run check-types",
    "format": "pnpm exec ultracite format",
    "prepare": "node .husky/install.mjs",
    "bump-ui": "pnpm dlx shadcn@latest add --all --overwrite -c packages/ui"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "@turbo/gen": "^2.4.4",
    "husky": "^9.1.7",
    "lint-staged": "^15.5.0",
    "prettier": "^3.5.3",
    "prettier-plugin-tailwindcss": "^0.6.11",
    "turbo": "^2.4.4",
    "ultracite": "4.2.0"
  },
  "packageManager": "pnpm@8.15.6",
  "engines": {
    "node": ">=18"
  }
}
